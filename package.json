{
  "name": "rembg-cv-maker",
  "version": "1.0.0",
  "description": "RemBG CV Maker - Professional JSON-driven CV Generator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "live-server --port=3000 --open=/index.html --watch=.",
    "server": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
